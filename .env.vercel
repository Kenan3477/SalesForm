# Vercel Environment Variables
# Copy these to your Vercel project settings

# Database (Required)
DATABASE_URL="postgresql://username:password@hostname:5432/database"

# Authentication (Required)
NEXTAUTH_SECRET="your-secret-key-generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="https://your-app.vercel.app"

# Optional - Node Environment
NODE_ENV="production"

# Example Database URLs:
# Vercel Postgres: postgres://default:xxxx@xxxx-pooler.vercel-storage.vercel.app:5432/verceldb
# Neon: postgresql://username:password@ep-xxxx.neon.tech/neondb
# Supabase: postgresql://postgres:password@db.xxxx.supabase.co:5432/postgres